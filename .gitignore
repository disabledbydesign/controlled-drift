.DS_Store
# Secrets — Anytype Local API key lives here. NEVER commit.
.env
# Machine-specific local settings (contains absolute paths to the Reframe install).
.claude/settings.local.json
# Reframe project state — local config + transient per-session state, not source. (2026-07-20:
# previously ".reframe/, .reframe-active IS tracked" — reversed; nothing here belongs in a
# public repo.)
.reframe/
.reframe-active
.gsdot

# Python
__pycache__/
*.pyc

# node — dependency tree, not source.
node_modules/

# Secret — a markdown copy of the Anytype API key. NEVER commit.
Anytype Key.md
# Runtime data the system writes at use time (not source).
scripts/data/

# Friction snapshots — PNGs of June's real screen (task names, medical/financial detail).
# Already covered by scripts/data/ above; named explicitly so the reason is on the record and
# a later narrowing of that rule cannot silently start committing her screenshots. (2026-07-19)
scripts/data/shots/

# Session-local / personal artifacts (June: ignore, 2026-07-12)
.gsdot-log.md
.claude/
Julia Task Home*.png

# Review-surface artifacts — GENERATED from live Anytype, carry June's real health &
# financial detail (savings, rent, healthcare timeline, surgery/recovery). LOCAL ONLY,
# never commit. The CODE that builds them (review_surface.py, surface_template.html) IS
# tracked; the rendered output and her export JSON are not. (2026-07-13)
docs/controlled_drift_tree.html
docs/controlled_drift_tree.md
docs/controlled_drift_tree_june_edits.md
docs/controlled_drift_changes.json

# Handoff/design docs quoting real health, financial, or relationship detail from June's
# Anytype space (surgery dates, disability paperwork, savings/rent figures, family caregiving).
# LOCAL ONLY, never commit — scrubbed from history 2026-07-16 before this repo's first push.
docs/survival_plan.md
docs/route_structure_assessment_2026-07-12.md
docs/focus_configuration_addendum.md
docs/discovery_D_open_pass.md
docs/handoff_2026-07-13_review_surface_pass.md
# Companion pull doc to survival_plan.md, same reason — missed in the 2026-07-16 pass.
# Real email, grant-budget figures, family/business detail. Scrubbed from history 2026-07-20.
docs/survival_plan_pulls_2026-07-12.md
# Seed script + its source worked example: real old work email, real savings/surgery-recovery
# detail hardcoded as seed/test data. Nothing else imports either file. Scrubbed 2026-07-20.
scripts/seed_jobsearch.py
prompts/examples/jobsearch_weeding_example.md
# Positionality section quotes an active legal matter June flagged as not-for-content, plus
# employment-history detail. Scrubbed from history 2026-07-20.
SYNTHESIS_design_time.md

# A private text conversation with June's sister (both real names) + the design doc built
# directly from it, and the entities list naming both of them. Julia hasn't given permission
# for any of this to be public. Scrubbed from history 2026-07-20.
Conversation with Julia.md
JULIA_FRAMEWORK.md
entities.json

# Reframe design-corpus doc, not Controlled Drift's own — flagged for June's OK, pulled
# preemptively pending that. Scrubbed from history 2026-07-20.
WORKSHOP_LOG.md

# ⚠ Generated review sheets that QUOTE June's real object content (task names, notes,
# medical/financial detail). Same reason scripts/data/ and the rendered tree are ignored:
# her data does not go to a git remote. The GENERATORS are tracked
# (scripts/migration_report.py, scripts/semantics_report.py); their output is not.
# Added 2026-07-18 after two such sheets were committed by mistake — caught before any push.
docs/*_migration_*_review.md
docs/context_cleanup_*_review.md
docs/side_rename_*_review.md
Controlled Drift.app/
scripts/desktop/AppIcon.iconset/
scripts/desktop/icon_master.png
scripts/desktop/appicon.icns
scripts/desktop/__pycache__/
scripts/__pycache__/
