.DS_Store
# Secrets — Anytype Local API key lives here. NEVER commit.
.env
# Machine-specific local settings (contains absolute paths to the Reframe install).
# The portable Reframe project config (.reframe/, .reframe-active) IS tracked.
.claude/settings.local.json
# Transient per-session Reframe state.
.reframe/session/

# Python
__pycache__/
*.pyc

# Secret — a markdown copy of the Anytype API key. NEVER commit.
Anytype Key.md
# Runtime data the system writes at use time (not source).
scripts/data/

# Session-local / personal artifacts (June: ignore, 2026-07-12)
.gsdot-log.md
.claude/
Julia Task Home*.png

# Review-surface artifacts — GENERATED from live Anytype, carry June's real health &
# financial detail (savings, rent, healthcare timeline, surgery/recovery). LOCAL ONLY,
# never commit. The CODE that builds them (review_surface.py, surface_template.html) IS
# tracked; the rendered output and her export JSON are not. (2026-07-13)
docs/controlled_drift_tree.html
docs/controlled_drift_tree.md
docs/controlled_drift_tree_june_edits.md
docs/controlled_drift_changes.json
