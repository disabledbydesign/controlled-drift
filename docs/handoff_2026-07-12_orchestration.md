# Orchestration handoff — 2026-07-12

**For the next orchestrating instance (any model).** June runs this session as high-level orchestration: she directs, the orchestrator keeps the whole-system view, verifies before trusting, routes work to agents, and protects against threads colliding or falling through. Partner stance, not deferential (see global CLAUDE.md; plain language, no metaphors, concept before label).

**Orient first:** `python3 scripts/whats_open.py` (live map) · `python3 scripts/describe_model.py` (live schema) · `docs/orchestrator_flags_2026-07-11.md` (verified findings each thread must read) · auto-memory `orchestration-2026-07-12.md`.

## Standing rule June set (2026-07-11)
Do NOT treat today's diagnosis docs — or the founding spec — as truth. Agent-written docs can misread the system; the spec can lag deliberate improvements. Every spec-vs-built divergence is adjudicated three ways: better-drift (ratify, update spec), undocumented-reason (recover + document), harmful-drift (fix). A ledger of these is being produced (see below). Verify diagnoses against the working tree before building on them.

## Verified state of the system (5-agent survey, 2026-07-11/12)
Multiple independent causes of the overwhelm — NOT one. Verified list lives in the flags doc; headline: selection reads almost no structure (owned thread); everything force-appends to the screen; capture writes ~3 of 14 fields (starves the planner); the learning layer is write-only (6 logs, 0 readers); neglect-resurfacing is non-functional 3 ways; prompts promise machinery that doesn't exist; no active focus period = pure hash order. Reliability: spec_reconciliation doc fully verified; structural_diagnosis partly refuted (its two read-bug claims are wrong; two DIFFERENT silent-key bugs are real — flags doc §selection #7).

## Board as of 2026-07-12 evening (supersedes the table below where they differ)
LANDED TODAY: selection gate (82→12 moves, live), capture-when (chips/verify/regenerate), capture-writes (duration/affect/blocked-on/access from June's words), duration intelligence (estimates labeled `estimated`, fidelity structural; backfill PREVIEW ready, `--run` June-gated), overlay tap-to-place move + summary rows (live, Anytype tasks closed), status-checker (live, monthly, Parked-exempt rule), content-free push, break formatting, memory-pass orphan adopted (8a8f67a), cross-family review now standing in repo CLAUDE.md. Suite: 452 green.
WAITING ON JUNE: (1) author a Focus Period — last definition-of-done item for ordering; (2) duration backfill skim → `python3 scripts/duration_backfill.py --run`; (3) memory-pass backfill preview skim → its `--run` + launchd slice; (4) route data entry go (crafts folded into leatherworking; moving/disability/finances routes at Open) — then the §8d navigator design session WITH June (merge with her survival-plan session; think-with-me register per spec §8d/stuck-support).
NEW CAPTURED: flaky test-sandbox leak (a test occasionally writes the real generation_log); format_preview count mismatch (cosmetic, duration_backfill); tests for adopted memory-pass logic.
DESIGN AMENDMENTS RECORDED: sprint ≠ foreground (reconciliation doc Layer 2 + Anytype task); routes are Projects + the Open engagement value carries possible-vs-active; §8d navigator is the missing layer, now unblocked by real route data.

## Threads
| Thread | Owner | State |
|---|---|---|
| Selection/engagement rewiring (step B of spec reconciliation) | June's parallel agent (other session) | mid-work; MUST read flags doc §selection (neglect broken, Steady default leak, rendering co-equal, 9 flags) |
| Capture-when → regenerate today | **BUILT ✅** (2026-07-12) — 390 tests green; live end-to-end verified (today/thursday/someday all anchored + read back correctly; chip reschedule works; Parked out of pool); handoff to selection thread honored (predicate shipped, exclusion NOT wired — theirs) | selection thread wires `when_resolve.is_scheduled_for_today_or_earlier` into `load_active_items`; one deliberate telemetry row left in generation_log.jsonl |
| Capture-writes (duration/affect/blocked-on/access at capture) | **BUILT ✅** (2026-07-12) — 410 tests green; live end-to-end proved fidelity case (a) on real objects (duration=10 not the flat 30, affect verbatim, blocked-on, access option resolved; the second item got NOTHING — no invented scope); memory_pass wired via the shared capture_fields helper with clean hunk separation | `capture_fields.py` is the single extension point for June's future kind-of-day fields; cross-family review pending (reviewer running) |
| Status-checker | **LIVE ✅** (2026-07-12, June approved) — first real run supervised: the gate DOWNGRADED an LLM-proposed auto-fix to a flag (5 open tasks → not demonstrably complete); monthly launchd schedule loaded; kickstart re-run proved no-re-nag; map notice renders | 1 pending question for June: is "Daily plan pipeline" actually finished? (`python3 scripts/status_check.py --flags` / `--resolve`) |
| Divergence ledger | orchestrator agent (launched 2026-07-12, opus) | writing docs/divergence_ledger_2026-07-12.md; output = questions for June + ratify/document/fix verdicts |
| Capture-writes fix | orchestrator agent (launched 2026-07-12, opus) | writing plan docs/superpowers/plans/2026-07-12-capture-writes.md; BUILD ONLY AFTER capture-when lands (same file + contract) |
| Learning-loops v1 | unlaunched | plan ready (2026-07-02) but review against the wiring audit first (8 log formats; corrections signal stalled since Jun 30) |
| Trust-layer prompt fixes (false promises, plan age) | unlaunched | gated on the divergence ledger — June: "rotation" may be superseded by Focus Periods, don't strip language before adjudicating |

## Decisions June made 2026-07-12 (recorded; do not re-ask)
- **Plan mirror to Anytype: YES**, as a cheap in — but no long-term accumulation of daily plans (nothing beyond what the learning loop needs). Design shape: ONE "today's plan" object updated idempotently each generation, not an object per day. Build after the selection thread lands (plan_generate.py is theirs).
- **Push privacy: DONE** — ntfy body is now content-free with the tap-to-overlay link preserved (commit db71758). The functionality June named as load-bearing (push that links to the overlay on her phone) is intact.
- **Dead fields: wire, don't drop.** ai_autonomous is valuable — it feeds "AI, do any open autonomous work" (agent-facing; not the daily plan). June does NOT need to assess each field; the orchestrator routes them: Task due date → selection thread (it IS the "nearest deadline" pick signal in the reconciled model); Project reaching_for → selection thread (key-bug fix, flags doc #7); ai_autonomous + relevant_docs → one small new agent-facing thread ("open autonomous work" query surface); Project description → waits for the Map-tab redesign thread; Access notes → waits for June's "what should a planner know about different kinds of days" retrieval (below).
- **Survival-plan home (§8d): worth doing** — shape: a session that first DOCUMENTS the plan content with June, then designs the architecture to hold it. Needs scheduling with her, not launching cold.
- **Countdown disc + completion sound: worth doing** — small build, queue after in-flight threads land (overlay_daily.html and task_actions.py are currently shared files).
- **Affect at capture: fidelity to June's stated scope** (corrected 2026-07-12 — an earlier "never spread across a weed" rule overcorrected). The affect on each item matches the scope she gave it: a feeling about one item lands on that item; a feeling stated about the whole dump lands on every item ("that's the truth"); nothing stated → blank. The prohibited move is the agent INVENTING scope in either direction. Plus a standing future requirement: June can add/change affect over time, and agents should notice-and-ask when things seem to have shifted (lands with the create-vs-update design).
- **Access conditions: 3 built tags stand for now.** June reframed the real question: "what would be helpful for an agent to know when making a plan for different kinds of days (not just accessibility)?" — she is retrieving that design work from another session that broke down; do NOT launch anything on it until she reports back.

## June-gated items still open
0. **Status-checker flag [2026-07-12-n2vqgjaa] resolution package ready:** verification found a mix — 1 task done-in-code (auto-marked complete per the decided asymmetry, commit da98b5c + 3 tests, read back, ding), 2 Phase-8 tasks correctly open (deferred-by-design backlog attached AFTER the stream was marked Done), 2 needing June's word: (a) task-move-without-LLM affordance — still wanted, or covered enough by reschedule? (b) sub-items display — expanded project rows show tasks, or workstream focus? PLUS orchestrator recommendation awaiting June's yes: teach the checker that Parked tasks don't count against a Done stream (one-line + test; prevents this flag re-firing monthly for every finished stream carrying deferred backlog). Resolve the flag with the evidence trail once she answers.
2. Create-vs-update capture design conversation (now carries the affect-upkeep requirement above).
3. Scheduling the survival-plan documentation session.
4. ~~June checking the broken session~~ RESOLVED 2026-07-12: that session is closed, nothing salvaged — the "planner knowledge for different kinds of days" question is NOT in flight anywhere. It moves to the ORCHESTRATOR WATCH-LIST: after the wiring threads land (selection + capture-when + capture-writes), evaluate against June's real usage whether the underlying need (LLM knowing enough to plan different kinds of days well) is met by the wired fields (engagement, affect, access conditions, blocked-on, duration, scheduled) or whether a category-design session with June is still owed. Do not let this fall through: it is the successor to her original access-conditions question.
3. Create-vs-update capture gap — design conversation (nothing recognizes "new information about an existing thing"; she named this).
4. Bigger, no urgency: Anytype-renders inversion; ntfy.sh privacy; Map tab scope (renders 1 of 45 projects); dead-on-both-ends fields (drop or build readers).
5. Older queue (docs inventory): memory-pass preview approval; completed-work Done confirmations; Drift-boundary conversation; spoon-parameters design session.

## Known hazards
- Shared files across threads: capture_generate.py + weeding contract (capture-when ∧ capture-writes — sequenced, not parallel); task_actions.py (capture-when ∧ status-checker — both additive, builder told to re-read before edit); daily_plan.py/plan_generate.py belong to the selection thread ONLY.
- Uncommitted work accumulating on feat/overlay-actionable (memory_pass.py +205 etc.) — commit at logical checkpoints.
- In-repo skills/drift mirror is behind the global ~/.claude/skills/drift (Jul 11) — global skills still unversioned (Anytype task exists).
